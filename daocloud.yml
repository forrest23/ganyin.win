
version: '2'

services: mongodb
  ganyin.win:
    image: daocloud/ci-node:0.12
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 3001:3001